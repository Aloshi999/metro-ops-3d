# skyline

Extra towers that are **not** Kenney `building-skyscraper-a` .. `building-skyscraper-e`.

## Lot contract
- Model unit: **~1**
- Lots: **16 m**
- Instance scale: **14.5**
- `instance`: true
- `unique_per_lot`: false
- `shared_materials`: true
- Interiors: 190 m High camera window/cutaway cards — **not** first-person rooms


Kenney 1u meshes use scale **14.5**. MegaKit `Building_Large_2` is not a Kenney 1u kit — Look may measure the first GLB later.

## On disk (`du -sh` = 43M)

- `Building_Large_2.gltf` + `Building_Large_2.bin` from Quaternius Downtown City MegaKit Standard (zip 224M at `/workspace/downloads/Downtown_City_MegaKit_Standard.zip`).
- Shared `T_*.png` URI targets + interior window cards (`T_dark_interior.png`, `T_lit_interior_*.png`).
- `License_Standard.txt` (CC0 1.0).

`Building_Small_1` and `Building_Medium_2_001` stay in `district_downtown/buildings/`. Further skyline variety: `assets/optional/bulk/` via GitHub Releases / LFS.
